title: "Tronloop"
description: "Batarya Kapasite Kayıp İzleme Sistemi — Biruni Üniversitesi EEE"

markdown: kramdown
kramdown:
  input: GFM

defaults:
  - scope:
      path: ""
    values:
      layout: "default"

plugins:
  - jekyll-relative-links

relative_links:
  enabled: true
  collections: true

exclude:
  - src/
  - "*.sh"
  - "*.py"
